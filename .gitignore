# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/

# secrets
.env
/tmp/

# pipeline artifacts (Stage A bundles, caches, loader state)
db/
.cache/
.load_state.json

# data (kept local; not committed)
laws/

# macOS
.DS_Store

.claude/
reports/

# Docling markdown cache — now lives under db/ (covered by db/ rule above)

# legacy paths (kept for safety during transition)
out/
extracted/
extracted_*/
bundle_*/