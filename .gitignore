# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/

# secrets
.env
/tmp/

# pipeline artifacts (Stage A bundles, caches, loader state)
out/
.cache/
.load_state.json

# data (kept local; not committed)
extracted/
laws/

# macOS
.DS_Store

.claude/
reports/

# extraction outputs (local runs; never committed)
extracted/
extracted_*/

# Docling markdown cache (large; regenerated on-demand)
md_cache/

# Stage A bundles (embeddings + chunks; loaded into MongoDB, not versioned)
bundle_*/
out/